# ============================================================================
# Этот файл был ПУСТЫМ (0 байт) в исходном проекте — видимо, содержимое
# случайно стёрлось, а рядом остался ekf_params.yaml.save (файл-бэкап,
# который обычно создаёт vim после аварийного завершения) с более полной
# версией конфигурации.
#
# Он не подключён ни к одному launch-файлу и заменён на:
#   project_start/config/ekf_local.yaml
#   project_start/config/ekf_global.yaml
#   project_start/config/navsat_transform.yaml
#
# Оставлен не пустым (эта заглушка), чтобы случайный `ros2 launch` с этим
# файлом сразу показал понятную ошибку/предупреждение, а не тихо использовал
# пустой конфиг. Можно удалить всю папку params/, если она больше не нужна.
# ============================================================================
